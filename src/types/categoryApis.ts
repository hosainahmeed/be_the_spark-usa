export interface SportCategory {
    _id: string,
    name: string,
    type: string,
    isDeleted: boolean,
    createdAt: string,
    updatedAt: string,
    eventCount: number
    category_image: string
}